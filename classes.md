# Classes

```js
class Car {
  
  constructor (model, engine) {
    this.model = model;
    this.engine = engine;
  }
  
  run (speed) {
    console.log('Car ' + model + ' is running at speed ' + speed);
  }
  
}
```
